@Library("shared-starter") _
pipeline {
    agent {
    kubernetes {
      label 'buildprep'
      yamlFile 'kubernetespod.yaml'
    }
  }
    stages {
        stage("Build Preparation") {
            steps {
                getProjectFromJob()
            }
        }
    }
}