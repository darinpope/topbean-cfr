pipeline {
  agent{
    kubernetes {
      label 'cfr-dpope'
      yamlFile 'kubernetespod.yaml'
    }
  }
  stages {
    stage("Build Prep") {
      steps {
        container("alpine-zip") {
          sh """
            zip --version
          """
        }
      }
    }
  }
}
